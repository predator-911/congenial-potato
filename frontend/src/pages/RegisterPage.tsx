import {useState} from 'react'; import {register} from '../api/auth'; import {useAuth} from '../hooks/useAuth'; import {useNavigate,Link} from 'react-router-dom'; import {ErrorAlert} from '../components/ErrorAlert';
export function RegisterPage(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[err,setErr]=useState(''); const {setUser}=useAuth(); const nav=useNavigate(); return <form className="auth" onSubmit={async e=>{e.preventDefault(); try{const r=await register(email,password); setUser(r.user); nav('/')}catch(x:any){setErr(x.message)}}}><h1>Register</h1><ErrorAlert message={err}/><input value={email} onChange={e=>setEmail(e.target.value)} placeholder="email"/><input value={password} onChange={e=>setPassword(e.target.value)} placeholder="password (10+ chars)" type="password"/><button>Create account</button><Link to="/login">Have an account?</Link></form>}
